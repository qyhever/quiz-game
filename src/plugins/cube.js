import Vue from 'vue'
import {
  /* eslint-disable no-unused-vars */
  Style,
  Loading,
  Scroll,
  DatePicker,
  SegmentPicker
} from 'cube-ui'

Vue
  .use(Loading)
  .use(Scroll)
  .use(DatePicker)
  .use(SegmentPicker)
